// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // No need to define Kotlin here if using plugin management in settings.gradle
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
